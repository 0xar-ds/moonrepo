fileGroups:
  configs:
    - 'tsconfig.json'
    - 'tsconfig.{build,spec}.json'
    - '*.config.{j,t}s'
  sources:
    - 'src/**/*.ts'
    - '!src/**/*.spec.ts'
  tests:
    - 'src/**/*.ts'
    - 'src/**/*.spec.ts'
    - 'tests/**/*.ts'
  outputs:
    - 'dist/**/*'
